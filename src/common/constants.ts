export const HOUR_IN_SECONDS = 60 * 60;
export const HOUR_IN_MS = HOUR_IN_SECONDS * 1000;
export const DAY_IN_SECONDS = HOUR_IN_SECONDS * 24;
export const DAY_IN_MS = DAY_IN_SECONDS * 1000;
export const YEAR_IN_DAYS = 365;
export const YEAR_IN_SECONDS = YEAR_IN_DAYS * DAY_IN_SECONDS;
